"""
This files contains the packages that are required for the project.

Packages
--------

scikit-learn==0.21.2
    scikit-learn is a Python module for machine learning built 
    on top of SciPy and is distributed under the 3-Clause BSD license.

joblib==0.13.2
    Joblib is a set of tools to provide lightweight pipelining 
    in Python for saving and loading the model package.

numpy==1.19.2
    NumPy is the fundamental package for scientific computing with Python.

pandas==1.1.3
    Pandas is a fast, powerful, flexible and easy to use open source
    data analysis and data manipulation tool,
    built on top of the Python programming language.

pytest==6.1.2
    The pytest framework makes it easy to write small tests, yet 
    scales to support complex functional testing for applications and libraries.

pytest-cov==2.10.1
    This plugin produces coverage reports.

inference-schema[numpy-support]==1.2.1
    Inference Schema is a JSON schema for defining machine learning models
    and their inputs and outputs.

lightgbm==3.2.1
    LightGBM is a gradient boosting framework that uses tree based learning algorithms.
"""

scikit-learn==0.21.2
joblib==0.13.2
numpy==1.19.2
pandas==1.1.3
pytest==6.1.2
pytest-cov==2.10.1
inference-schema[numpy-support]==1.2.1
lightgbm==3.2.1
